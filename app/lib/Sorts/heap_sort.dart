import 'comparator.dart';

/// Performs a heap sort.
class HeapSort
{
  /// Performs a heap sort on the given list using the given comparator.
  static List<E> sort<E>(Iterable<E> list,  ComparatorValue Function(E, E) compare)
  {
    // TODO: Implement heap sort.
    return list.toList();
  }
}